spring:
  profiles:
    active: prd
  jpa:
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: update
    open-in-view: true
    
server:
  port: 8080
  servlet:
    encoding:
      charset: UTF-8
    context-path: /